schema_version: "0.1"
retrievals:
  case_search:
    search: vector
    candidate_limit: 10
    target_roles: [case_version, image_version]
    eligibility:
      valid_at_task_time: true
    resolution:
      policy: official_case_source
      accepted_outcomes: [selected]
    display_name: Multimodal clinical case + image semantic search
