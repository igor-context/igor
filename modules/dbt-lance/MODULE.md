# dbt-lance module

Reserve this capability for a future optional dbt compatibility layer. It is not part of the v0.1 runtime and exposes no callable interface. Relational models, materialization, lineage, and tests are owned by `modules/relational/` through the IGOR-native runner.
