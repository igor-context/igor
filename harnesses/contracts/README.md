# Contract harness

Planned home for cross-implementation fixtures that validate versioned benchmark inputs and run artifacts through public contracts. It will activate with the first canonical artifact schema and must remain independent of reference-tool implementations.
